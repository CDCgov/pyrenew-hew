% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_reporting_data_epidatr.R
\name{fetch_nhsn_data}
\alias{fetch_nhsn_data}
\title{Fetch NHSN hospital admissions data}
\usage{
fetch_nhsn_data(
  signal = "confirmed_admissions_covid_ew_prelim",
  geo_type = "state",
  geo_values = "*",
  time_values = "*",
  issues = "*"
)
}
\arguments{
\item{signal}{Character string specifying the NHSN signal to fetch.
Default is \code{"confirmed_admissions_covid_ew_prelim"}.}

\item{geo_type}{Character string specifying geographic resolution.
Options: \code{"state"}, \code{"hhs"}, \code{"nation"}. Default is \code{"state"}.}

\item{geo_values}{Character vector of geography codes or \code{"*"} for all.
Default is \code{"*"}.}

\item{time_values}{A timeset object (e.g., \code{epirange(202401, 202404)})
or \code{"*"} for all available dates. Default is \code{"*"}.}

\item{issues}{A timeset object for specific issue dates, or \code{"*"} for most
recent. Default is \code{"*"}.}
}
\value{
A tibble with columns including:
\itemize{
\item \code{geo_value}: Geographic identifier
\item \code{time_value}: Reference date (week-ending Sunday from Epidata)
\item \code{value}: Number of confirmed admissions
\item \code{issue}: Issue date (week-ending Sunday of the epiweek when data was
published)
\item \code{lag}: Days between reference date and issue date
\item \code{signal}: Signal name
\item Additional metadata columns from \code{epidatr::pub_covidcast()}
}
}
\description{
Fetches target NHSN (National Healthcare Safety Network) hospital respiratory
admissions data from the \code{epidatr} R client for the Delphi Epidata API. This
is just a closure around \code{epidatr::pub_covidcast()} with validation for the
target signals and parameters used in this nowcasting and checking that the
output is non-empty.
NB:
\itemize{
\item For \code{time_values} and \code{issues}, you can pass an \code{epidatr::timeset} object
however this source only supports epiweeks (not dates).
\item Output week dates are converted from Sundays (Epidata) to Saturdays to
align with reporting dates from forecast hub data.
}
}
